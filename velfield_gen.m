function velfield_gen

%% Load in data (depends on FIDIC output format). Output of section: cell vectors U and V with X and Y displacement matrices. 

%% Generate instantaneous velocity matrix cell vectors P and Q for X and Y 

%% Collect circle data

%% Create location grids - use circle data to scale based on radius

%% Convert matrices to one of [x y u v]

%% Convert velocity to polar - output [r theta rho], can throw away radial velocity

%% loop for spatial averaging inside loop for time averaging
end